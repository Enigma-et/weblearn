import { createFileRoute } from '@tanstack/react-router'
import WebDevCurriculum from '../../complete-webdev-curriculum.jsx'

export const Route = createFileRoute('/')({ component: WebDevCurriculum })
